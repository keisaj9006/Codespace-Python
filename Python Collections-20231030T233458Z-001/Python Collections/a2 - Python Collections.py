# Define the sample list
sample_list = [20, 30, 25, 35, -16, 60, -100]

# Calculate average
list_sum = sum(sample_list)
list_length = len(sample_list)
average = list_sum / list_length

# Print the result rounded to one decimal place
print(f"Average value of the list elements is: {average:.1f}")